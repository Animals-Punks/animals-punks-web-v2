import { ElementBox, CenterText, TitleText } from "@/common/components";

const RarityBox: React.FC = () => {
    return (
        <ElementBox backgroundColor="white">
            <CenterText>
                <TitleText textColor="#000">RARITY</TitleText>
            </CenterText>
        </ElementBox>
    );
};

export default RarityBox;
